{
  "name": "arqam",
  "version": "0.1.0",
  "description": "",
  "license": "MIT",
  "author": "Theo Beers",
  "main": "arqam.js",
  "scripts": {
    "build-css": "postcss styles.css -o compiled.css",
    "format": "prettier --write --plugin-search-dir=. .",
    "lint": "standard --fix",
    "minify": "esbuild arqam.js --minify --sourcemap --outfile=arqam.min.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.0",
    "esbuild": "^0.14.2",
    "postcss": "^8.4.4",
    "postcss-cli": "^9.0.2",
    "prettier": "^2.5.1",
    "standard": "^16.0.4",
    "tailwindcss": "^2.2.19"
  }
}
