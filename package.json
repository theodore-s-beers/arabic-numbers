{
  "name": "arqam",
  "version": "0.1.0",
  "description": "",
  "license": "MIT",
  "author": "Theo Beers",
  "main": "js/arqam.js",
  "scripts": {
    "css": "tailwindcss -i css/styles.css -o css/compiled.min.css --minify",
    "dev": "tailwindcss -i css/styles.css -o css/compiled.min.css --minify --watch",
    "format": "prettier --write .",
    "lint": "eslint --ignore-path .prettierignore --fix .",
    "minify": "esbuild js/arqam.js --minify --sourcemap --outfile=js/arqam.min.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "eslint": "^8.56.0",
    "prettier": "^3.2.4",
    "prettier-plugin-tailwindcss": "^0.5.11",
    "tailwindcss": "^3.4.1"
  }
}
